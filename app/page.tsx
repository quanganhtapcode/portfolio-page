import type { CSSProperties } from "react";
import { loadPortfolioContent } from "@/lib/portfolio-content";

export const dynamic = "force-dynamic";
const Arrow = () => <span className="arrowIcon" aria-hidden="true" />;

export default async function Home() {
  const content = await loadPortfolioContent();
  const { settings } = content;
  const typeStyle = { "--hero-scale": settings.heroScale, "--heading-scale": settings.headingScale, "--body-scale": settings.bodyScale, "--about-eyebrow-scale": settings.aboutEyebrowScale, "--about-eyebrow-tracking": settings.aboutEyebrowTracking, "--research-title-scale": settings.researchTitleScale, "--experience-title-scale": settings.experienceTitleScale } as CSSProperties;
  return <main style={typeStyle}>
    <nav className="nav"><a className="brand" href="#top">LA<span>®</span></a><div className="navLinks"><a href="#about">About</a><a href="#work">Portfolio</a><a href="#research">Research</a><a href="#experience">Experience</a><a href="#education">Education</a></div><a className="contact" href="https://www.linkedin.com/in/quanganhday/" target="_blank" rel="noreferrer">LinkedIn <Arrow /></a></nav>
    <section id="top" className="hero"><div className="heroCopy"><div className="metrics"><span><b>3.67</b> GPA / 4.0</span><span><b>Top 3</b> Finance program</span></div><h1 aria-label={settings.heroGreeting}>{[...settings.heroGreeting].map((letter, index) => <span className={letter === "." ? "helloDot" : "helloLetter"} key={`${letter}-${index}`}>{letter}</span>)}</h1><p className="signature">{settings.heroDescription}</p><div className="heroLinks"><a href="#work">Scroll down <Arrow /></a><a href="mailto:quanganh.ibd@gmail.com">Let&apos;s work together <Arrow /></a></div></div><div className="portrait"><div className="portraitFrame"><img src="/profile-cutout.png" alt="Le Quang Anh" /></div><p>Hanoi, Vietnam<br/>CFA Level II Candidate</p></div></section>
    <section className="intro" id="about"><p className="eyebrow aboutEyebrow">{settings.aboutEyebrow}</p><div><h2>{settings.aboutHeadingPrimary}<br/><em>{settings.aboutHeadingAccent}</em></h2><p>{settings.aboutDescription}</p></div></section>
    <section className="work" id="work"><div className="workHead"><p className="eyebrow">{settings.workLabel}</p><p>{settings.workRange}</p></div><article className="feature"><div><p className="tag">{settings.workTag}</p><h2>{settings.workTitlePrimary}<br/>{settings.workTitleAccent}</h2><p>{settings.workDescription}</p><a href="https://stock.quanganh.org" target="_blank" rel="noreferrer">{settings.workLinkLabel} <Arrow /></a></div><div className="modelGrid"><span>FCFE</span><span>FCFF</span><span>J. P/E</span><span>J. P/B</span></div></article></section>
    <section className="research" id="research"><div className="researchHead"><p className="eyebrow">{settings.researchLabel}</p><h2>{settings.researchHeadingPrimary}<br/>{settings.researchHeadingAccent}</h2></div><div className="researchList">{content.research.map((item, index) => <article key={`${item.title}-${index}`}><span>{String(index + 1).padStart(2, "0")} — {item.status}</span><div><p className="researchMeta">{item.meta}</p><h3>{item.title}</h3><p>{item.description}</p>{item.link && <a href={item.link} target="_blank" rel="noreferrer">{item.linkLabel || "Read more"} <Arrow /></a>}</div></article>)}</div></section>
    <section className="experience" id="experience"><p className="eyebrow">{settings.experienceLabel}</p>{content.experience.map((item, index) => <article key={`${item.role}-${index}`}><p>{String(index + 1).padStart(2, "0")}</p><div><span>{item.dates}</span><h3>{item.role}</h3><b>{item.company}</b></div><p>{item.description}</p></article>)}</section>
    <section className="education" id="education"><div><p className="eyebrow">{settings.educationLabel}</p><h2>{settings.educationHeadingPrimary}<br/>{settings.educationHeadingAccent}</h2></div><div className="credentials"><article><span>2025</span><h3>BBA, Finance</h3><p>VNU University of Economics &amp; Business<br/>GPA 3.67 / 4.0 · Top 3 of program</p></article><article><span>In progress</span><h3>CFA Level II Candidate</h3><p>Chartered Financial Analyst Program</p></article><article><span>Toolkit</span><h3>Financial research &amp; modelling</h3><p>DCF · Comparable companies · Excel · Python · Flask · Bloomberg</p></article></div></section>
    <footer><p>{settings.footerHeading.split("\n").map((line, index) => <span key={`${line}-${index}`}>{line}{index < settings.footerHeading.split("\n").length - 1 && <br/>}</span>)}</p><a href="mailto:quanganh.ibd@gmail.com">quanganh.ibd@gmail.com <Arrow /></a><div><span>© {new Date().getFullYear()} Le Quang Anh</span><a href="https://quanganh.org" target="_blank" rel="noreferrer">quanganh.org</a></div></footer>
  </main>;
}